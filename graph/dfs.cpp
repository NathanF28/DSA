#include <iostream>
#include <vector>
using namespace std;

void DFS(int node, vector<vector<int>> &graph, vector<bool> &visited) 
{
    int V;
    for (int i=0; i < V;i++) {
        visited[i] = false;
    }
    start[s] 
} 